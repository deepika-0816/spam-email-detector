# spam-email-detector
A machine learning-based application that classifies email or SMS messages as spam or not spam. The model uses text preprocessing techniques and a Naive Bayes classifier trained on the SMS Spam Collection Dataset.
